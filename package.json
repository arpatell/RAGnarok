{
  "name": "panelflow",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently --kill-others \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:rag\"",
    "dev:backend": "npm run dev --workspace @panelflow/backend",
    "dev:frontend": "npm run dev --workspace @panelflow/frontend",
    "dev:rag": "node scripts/dev-rag.mjs",
    "build": "npm run build --workspace @panelflow/backend && npm run build --workspace @panelflow/frontend",
    "typecheck": "npm run typecheck --workspace @panelflow/backend && npm run typecheck --workspace @panelflow/frontend"
  },
  "devDependencies": {
    "concurrently": "^9.0.1",
    "typescript": "^5.6.3"
  }
}
